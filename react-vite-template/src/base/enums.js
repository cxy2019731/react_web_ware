// 静态枚举-不可修改
import { ENUM_LAYOUT_MODE } from './constant';
export const static_enum = {
	[ENUM_LAYOUT_MODE]: [
		{ name: '左菜单模式', value: 'leftMenu' },
		{ name: '顶部菜单模式', value: 'topMenu' },
		{ name: '左菜单混合模式', value: 'leftMixin' },
	],
};
